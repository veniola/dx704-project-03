Based on my investigation, my choice of multi-armed bandit algorithm is epsilon-greedy because the observed rewards are continuous, non-negative real numbers that are not bounded in [0, 1], which rules out algorithms like Bernoulli rewards and Thompson sampling (which require rewards {0,1}) and UCB (which assumes rewards [0, 1]), while epsilon-greedy only needs sample-mean reward estimates and has no range restriction.